{% extends 'base.html.twig' %}

{% block title %}Nos services{% endblock %}

{% block body %}
    <section class="services" id="services">

        <div class="heading">
            <h1>Nos services</h1>
            <p>Des services de qualité pour vous satisfaire</p>
            {% for service in services %}
            <div class="container">
                <div class="image"><img src="{{ asset('uploads/photoServices/' ~ service.imageName) }}" alt=""></div>
                <div class="content">
                    <h2 class="title">{{ service.title }}</h2>
                    <p class="text">{{ service.description }}</p>
                    <p></p>
                </div>
            </div>
            {% endfor %}
        </div>

    </section>

{% endblock %}
